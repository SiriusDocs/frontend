# frontend

**Environment variables**
In the root of the project create .env:

```
VITE_API_URL=http://localhost:8080
```

Use to install all dependences

```
npm install
```

Then, use to build project

```
npm run build
```
